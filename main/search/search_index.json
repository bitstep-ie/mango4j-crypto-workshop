{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Mango4j Crypto Workshop","text":"<p>A hands-on, step-by-step workshop for mango4j-crypto \u2014 a framework for implementing Application Level Encryption in Java applications.</p> <p>Each stage is a self-contained, standalone Maven project in its own folder \u2014 not something you build up with git. You progress by moving into one stage's folder at a time, trying things out, then moving on to the next.</p>"},{"location":"#prerequisites","title":"Prerequisites","text":"<ul> <li>JDK 17+</li> <li>Maven</li> </ul>"},{"location":"#how-to-follow-along","title":"How to follow along","text":"<p>Clone the workshop repository, then move into stage 1's folder:</p> <pre><code>git clone https://github.com/bitstep-ie/mango4j-crypto-workshop.git\ncd mango4j-crypto-workshop/stages/stage-01\n</code></pre> <p>Each stage's page tells you which folder to move into next, e.g.:</p> <pre><code>cd ../stage-02\n</code></pre> <p>Using an IDE?</p> <p>Each <code>stages/stage-NN/</code> folder is a standalone Maven project, so instead of <code>cd</code>-ing on the command line, you can just open that folder as its own project in your IDE (e.g. \"Open\" \u2192 <code>stages/stage-02</code> in IntelliJ/Eclipse/VS Code).</p> <p>Because every stage is its own independent folder, anything you change while experimenting in one stage has no effect on the next \u2014 there's nothing to discard, reset, or merge.</p> <p>The code samples shown on each page are pulled directly from that stage's folder, so what you read here always matches what's on disk.</p>"},{"location":"#stages","title":"Stages","text":"<ol> <li>Getting Started</li> <li>Encrypting a Field</li> </ol>"},{"location":"how-it-works/","title":"How This Workshop Works","text":"<p>This page explains the mechanics behind the workshop, for anyone contributing a stage or just curious how the pieces fit together.</p>"},{"location":"how-it-works/#stages-are-independent-standalone-projects","title":"Stages are independent, standalone projects","text":"<p>Each stage of the workshop lives in its own folder \u2014 <code>stages/stage-01/</code>, <code>stages/stage-02/</code>, and so on \u2014 and each folder is a complete, standalone, buildable Maven project, not a diff or a patch on the previous stage.</p> <p>As a learner, you move through the workshop by moving into one stage's folder, trying its exercise, then moving into the next stage's folder:</p> <pre><code>cd stages/stage-01\n# ...try the exercise...\ncd ../stage-02\n</code></pre> <p>Using an IDE?</p> <p><code>cd</code> is just the command-line way to do it. Each <code>stages/stage-NN/</code> folder is a standalone Maven project, so you can equally open it directly as its own project in an IDE (e.g. \"Open\" \u2192 <code>stages/stage-02</code> in IntelliJ/Eclipse/VS Code) instead of <code>cd</code>-ing.</p> <p>Because each stage's folder is entirely separate from the others, there's no git branching, merging, or resetting involved in moving between them. Whatever you change while experimenting in <code>stage-01</code> simply has no way to reach <code>stage-02</code> \u2014 it's a different folder.</p> <p>Under the hood, later stages are typically authored by copying the previous stage's folder and adding that stage's changes, so <code>diff -r stages/stage-01 stages/stage-02</code> shows exactly what changed. That's an authoring detail, not something learners need to know.</p>"},{"location":"how-it-works/#code-samples-are-pulled-live-from-the-stage-folders","title":"Code samples are pulled live from the stage folders","text":"<p>Every code block on a stage's page in this docs site is not hand-copied \u2014 it's pulled directly out of that stage's actual folder at build time, so the docs can never drift out of sync with code that really compiles.</p> <p>This is done with the <code>pymdownx.snippets</code> Markdown extension, configured in <code>mkdocs.yml</code> with <code>stages/</code> as its search path. A stage's source file can mark the interesting part of itself with comments:</p> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;ie.bitstep.mango&lt;/groupId&gt;\n    &lt;artifactId&gt;mango4j-crypto&lt;/artifactId&gt;\n    &lt;version&gt;1.0.0&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre> <p>And a docs page includes just that section with:</p> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;ie.bitstep.mango&lt;/groupId&gt;\n    &lt;artifactId&gt;mango4j-crypto&lt;/artifactId&gt;\n    &lt;version&gt;1.0.0&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre> <p>The path is relative to <code>stages/</code>, and <code>:dependency</code> selects only the text between the matching <code>[start:dependency]</code> / <code>[end:dependency]</code> markers \u2014 the rest of the file (boilerplate, unrelated config) is left out. Dropping the <code>:label</code> includes the whole file instead. Marker labels only need to be unique within their own file, so pick something descriptive per snippet (<code>dependency</code>, <code>encrypt-annotation</code>, <code>key-provider</code>, ...).</p>"},{"location":"how-it-works/#why-this-way-instead-of-hand-written-code-blocks","title":"Why this way, instead of hand-written code blocks?","text":"<p>A hand-copied snippet can silently drift from the real code the moment either one changes. Pulling straight from the stage's folder means that's structurally impossible \u2014 if a stage's <code>pom.xml</code> changes, its docs page picks up the new content the next time the site builds, with no separate step to remember.</p>"},{"location":"stages/01-getting-started/","title":"1. Getting Started","text":"<p>This stage sets up a Maven project with the <code>mango4j-crypto</code> dependency already in place, ready to start encrypting entity fields in the next stage.</p> <p>Follow along</p> <p><pre><code>cd stages/stage-01\n</code></pre> Using an IDE instead? Just open <code>stages/stage-01</code> as its own project.</p>"},{"location":"stages/01-getting-started/#the-dependency","title":"The dependency","text":"<p><code>stages/stage-01/pom.xml</code> already has <code>mango4j-crypto</code> declared:</p> <pre><code>&lt;dependency&gt;\n    &lt;groupId&gt;ie.bitstep.mango&lt;/groupId&gt;\n    &lt;artifactId&gt;mango4j-crypto&lt;/artifactId&gt;\n    &lt;version&gt;1.0.0&lt;/version&gt;\n&lt;/dependency&gt;\n</code></pre> <p>That's it \u2014 this project can now use the <code>@Encrypt</code>, <code>@Hmac</code>, <code>@EncryptedData</code>, <code>@EncryptionKeyId</code> and <code>@HmacKeyId</code> annotations that <code>mango4j-crypto</code> provides.</p> <p>Feel free to experiment here \u2014 each stage is its own standalone folder, so nothing you change in <code>stage-01</code> affects <code>stage-02</code>. When you're ready, just move on:</p> <pre><code>cd ../stage-02\n</code></pre> <p>Next: 2. Encrypting a Field.</p>"},{"location":"stages/02-encrypting-a-field/","title":"2. Encrypting a Field","text":"<p>In this stage you'll encrypt a single field \u2014 a card number \u2014 using <code>mango4j-crypto</code>.</p> <p>Follow along</p> <p><pre><code>cd stages/stage-02\n</code></pre> Using an IDE instead? Just open <code>stages/stage-02</code> as its own project.</p>"},{"location":"stages/02-encrypting-a-field/#the-entity","title":"The entity","text":"<p><code>mango4j-crypto</code> works by annotating fields on a plain Java object. <code>PaymentCardEntity</code> has two fields that matter:</p> <pre><code>@Encrypt\nprivate transient String cardNumber;\n</code></pre> <p><code>@Encrypt</code> marks <code>cardNumber</code> as confidential \u2014 note it must be <code>transient</code>, which the library enforces. Its plaintext value is never written to <code>encryptedData</code> or stored anywhere by the library itself; it just stays as a normal, in-memory value on the object you're holding.</p> <pre><code>@EncryptedData\nprivate String encryptedData;\n</code></pre> <p><code>@EncryptedData</code> marks where the resulting ciphertext goes. This is the field you'd actually persist (to a database, a file, wherever) \u2014 never <code>cardNumber</code> itself.</p>"},{"location":"stages/02-encrypting-a-field/#wiring-up-cryptoshield","title":"Wiring up CryptoShield","text":"<p><code>CryptoShield</code> is the object you call <code>encrypt()</code>/<code>decrypt()</code> on. Building one needs two things: something that supplies cryptographic keys (a <code>CryptoKeyProvider</code>), and something that does the actual encrypting (an <code>EncryptionServiceDelegate</code>).</p> <p>For this stage, <code>InMemoryCryptoKeyProvider</code> hands back one hardcoded key \u2014 a real application would look keys up from wherever it stores them. And rather than wiring up real encryption (KMS, a cipher, ...), we use the library's built-in <code>Base64EncryptionService</code>, which just Base64-encodes data \u2014 it exists specifically so you can learn and test the mechanics without any real cryptographic setup.</p> <pre><code>CryptoShield cryptoShield = new CryptoShield.Builder()\n        .withCryptoKeyProvider(new InMemoryCryptoKeyProvider())\n        .withAnnotatedEntities(List.of(PaymentCardEntity.class))\n        .withEncryptionServiceDelegates(List.of(new Base64EncryptionService()))\n        .build();\n</code></pre>"},{"location":"stages/02-encrypting-a-field/#encrypting-and-decrypting","title":"Encrypting and decrypting","text":"<pre><code>PaymentCardEntity card = new PaymentCardEntity();\ncard.setCardNumber(\"4111111111111111\");\n\ncryptoShield.encrypt(card);\nSystem.out.println(\"cardNumber (still in memory): \" + card.getCardNumber());\nSystem.out.println(\"encryptedData:                \" + card.getEncryptedData());\n</code></pre> <p><code>encrypt()</code> reads the <code>@Encrypt</code> field(s), builds the ciphertext, and writes it to the <code>@EncryptedData</code> field \u2014 <code>cardNumber</code> itself is left untouched, so you can keep using it in your code right after encrypting.</p> <pre><code>// Simulate loading the entity back from storage: only the ciphertext is known.\nPaymentCardEntity loaded = new PaymentCardEntity();\nloaded.setEncryptedData(card.getEncryptedData());\n\ncryptoShield.decrypt(loaded);\nSystem.out.println(\"decrypted cardNumber:         \" + loaded.getCardNumber());\n</code></pre> <p><code>decrypt()</code> does the reverse: given only the ciphertext, it reconstructs <code>cardNumber</code>. This is what \"loading an entity back from storage\" looks like in practice \u2014 you'd load a row containing only <code>encryptedData</code>, then call <code>decrypt()</code> to get the real value back.</p> <p>Run it and you'll see something like:</p> <pre><code>cardNumber (still in memory): 4111111111111111\nencryptedData:                {\"cryptoKeyId\":\"workshop-encryption-key\",\"data\":{\"cipherText\":\"eyJjYXJkTnVtYmVyIjoiNDExMTExMTExMTExMTExMSJ9\"}}\ndecrypted cardNumber:         4111111111111111\n</code></pre> <p>Next: 3. Coming soon.</p>"}]}
# 2. Key Aliases → Crypto Key Configs

*Content coming soon.*

- What a key alias is and why application code refers to keys by alias, not by raw key material or provider details
- How an alias resolves to a concrete crypto key config (provider, algorithm, key identity)
- Why this indirection is what makes key rotation and multi-provider support possible later in the talk
